optimizer:
  type: "adam"
  params:
    lr: 0.0001

scheduler:
  type: "none"
  params:
    mode: "min"
    factor: 0.3
    patience: 10
    threshold: 0.001
    min_lr: 0.00001

seed: 0

num_epochs: 500
